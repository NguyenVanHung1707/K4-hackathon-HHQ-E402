FROM node:20-alpine

WORKDIR /app

# Copy package.json and install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose Vite default port
EXPOSE 5173

# Run Vite development server on all network interfaces
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
